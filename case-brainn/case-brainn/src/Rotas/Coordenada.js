export const verMegaSena = (history, id) => {
    history.push("/");
  };
  
  export const verQuina = (history) => {
    history.push("/quina");
  };
  
  export const verLotoFacil = (history) => {
    history.push("/lotofacil");
  };
  
  export const verLotoMania = (history) => {
    history.push("/lotomania");
  };
  
  export const verTimeMania = (history) => {
    history.push("/timemania");
  };
  
  export const verDiaDeSorte = (history) => {
    history.push("/diadedosorte");
  };

  // export const verDiaDeSorte = (history) => {
  //   history.push(`/diadedosorte/${id}`);
  // };